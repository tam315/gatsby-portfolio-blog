import React from 'react'

import { rhythm } from '../../utils/typography';

const IconExtLink = ({ color = '#999999' }) => (
  <i className="fas fa-external-link-alt" css={{
    color,
    height: '0.8rem',
    verticalAlign: '0%',
  }}></i>
)

export default IconExtLink
